# Make better-sqlite3 download its prebuilt binary for Electron (not Node),
# so no C++ compiler / node-gyp build is required.
runtime=electron
target=33.4.11
disturl=https://electronjs.org/headers
build_from_source=false
